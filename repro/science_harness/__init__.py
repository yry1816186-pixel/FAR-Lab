"""FAR-Chain science harness repro helpers (P1-6).

sandbox_runner.py — deterministic venv sandbox executor (threadpoolctl nthread=1 + fixed seed).
dataset_fetch.py — online dataset fetcher (lightkurve / astroquery.mast, host-whitelisted).
"""
