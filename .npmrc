engine-strict=true
# Supply-chain hardening (borrowed from pi/earendil-works): pin exact versions
# so installs are reproducible; lockfile is the dependency ground truth.
# Note: npm-only option min-release-age is not supported by pnpm — the check
# script scripts/check-supply-chain.mjs enforces exact pins instead.
save-exact=true
