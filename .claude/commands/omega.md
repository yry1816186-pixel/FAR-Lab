---
description: FAR-Lab Ω-ULTRA 终局重构（ultrathink 级，reuse-first，系统 Wave 推进）
---

ultrathink

读取并立即执行仓库根目录 `OMEGA-ULTRA-CONTRACT.md`——它是你本次会话唯一的最髙任务书，本命令以最高推理档位触发它。仓库既有安全、凭据、许可证红线继续有效。

启动序列（不询问，直接做）：

1. Read `OMEGA-ULTRA-CONTRACT.md` 全文，接受其全部约束（Death List / 验证批处理 / reuse-first / Wave 门禁）。
2. Read `.control/OMEGA-MISSION-STATE.md`：存在 → 从 EXACT NEXT ACTION 恢复续跑；不存在 → 从 Wave 0 开始，并创建该文件。
3. 按 PART 4 REALITY MAP 建立/更新真实地图（以契约内仓库锚点为起点，不重新摸索），产出 3–5 个系统级瓶颈 + 替代方案假设。
4. 进入 Wave 执行循环：并行 subagent fan-out（搜索/克隆/评测/审查），主 Agent 融合裁决；Wave 出口跑一次全量门禁；摘要写入 MISSION STATE。
5. 结束回合的条件只有一个：当前可执行工作真正完成或遇到只有用户能解的阻塞。上下文将满时先保存 MISSION STATE。

工作纪律：不逐文件修补、不逐个跑测试、不手写已有工业级实现的库；先 SEARCH/CLONE/RUN/FUSE 再谈 BUILD；一切能力声明配命令级证据。

$ARGUMENTS
