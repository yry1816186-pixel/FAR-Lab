{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability: the shell only shows the workbench webview; no native APIs are exposed to page code.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
