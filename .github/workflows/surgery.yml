name: surgery

# Remote-surgery patch plane — DORMANT (post-slice-4 neutralization,
# 2026-08-29). Reactivated three times, always branch-scoped to one
# converge/** slice and triggered only by the scripts/surgery/.diag-trigger
# marker:
#   slice 2 (converge/web-protocol, PR #133) — apply-web-protocol.mjs
#   slice 3 (converge/protocol-cli, PR #134) — apply-cli-protocol.mjs
#   slice 4 (converge/export-protocol, PR #135) — apply-protocol-export.mjs:
#     six insert-only edits routing the protocol chain into the export plane
#     (provenance/verify/export/paper-outline/package; bot commit df7f5b7,
#     evidence in scripts/surgery/apply-log.txt).
# Mechanism + hardening: see scripts/surgery/README.md.
# Validity: a workflow must declare at least one job (the zero-job form was
# invalid and registered a phantom failure run per carrying push — #22-#25).
# Known defect (registered, slice-5 candidate): the diagnose job's snapshot
# step lacks `if: always()`, so an early crash (runs #30/#31, 48s) leaves NO
# diag.txt — the next reactivation must add it.
on:
  workflow_dispatch:
jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'surgery patch plane dormant — see scripts/surgery/README.md'
