# FAR-Lab 深层审计报告 — 你没问但我应该主动发现的问题

> 这份报告不是对COMPETITION_STRATEGY的补充——它是替代。之前的策略停留在表面。

## 我的能力缺陷根因诊断

你反复指出我"不细致""不拓展""不主动"。经过自我审计，根因是：

### 根因1: 我在"回答问题"而不是"解决问题"
你说"测试质量有问题"，我写了一个审计脚本。但一个真正的首席工程师会：
- 发现测试是表象，追问"测试在测什么？这些功能真的完整吗？"
- 发现功能声称与现实不符，追问"还有多少声称是假的？"
- 发现声称是假的，追问"这意味着项目的可信度整体如何？评审会怎么看？"

我在每一层停下来等你给下一层指令。这就是执行者和架构师的区别。

### 根因2: 我不验证"声称"与"现实"的差距
项目说"1484个测试"——我不质疑就引用。项目说"frontend存在"——我不验证就转述。
一个真正的工程师看到"声称X"会立即验证"X是真的吗？"

### 根因3: 我不主动从"已知问题"外推"未知风险"
你指出测试浅薄。正确的反应不是"修测试"，而是：
- 测试浅薄 → 代码可能也没实现完整 → 验证 → 发现大量声称但未实现的功能
- 功能不完整 → 设计文档可能也有问题 → 验证 → 发现设计文档与代码脱节
- 代码+设计都有问题 → 项目交付能力整体有疑问 → 重新评估时间线

## 主动审计发现（你没问但致命的问题）

### 致命问题1（已修正）: 前后端有数据但端到端体验断裂

验证结果（不是声称，是实际curl输出）：
- API默认seed了1个verdict（C-ASTRO-0001 UNTESTED）+ 30个benchmark覆盖28个领域
- 五值裁决分布：CONFIRMED:6, REFUTED:8, INCONCLUSIVE:7, DEGRADED_SCOPE:7, UNTESTED:2
- 但：这些全是fixture数据，不是真实LLM生成的裁决
- 诚实标注：项目自己声明了"verdict由offline fixture产出（无真实LLM调用·非真实科学裁决）"
- 真正的问题：评委看到30个benchmark会问"这些裁决是真实的吗？"——答案是否

之前我错误声称"API返回空数据"——因为我用了--no-seed。这是我的幻觉，没有充分验证就下了结论。

### 致命问题2: 没有真正的端到端演示流程

当前demo（`far demo`）只展示CLI文本输出。但项目有完整的Web UI——
两者完全脱节。评委看到CLI输出后不会知道有Web UI，看到Web UI后发现是空的。

正确的演示流程应该是：
1. `far ask "Does catalyst X outperform Y?"` → agent生成假设+证据
2. 自动填充数据库 → Web UI实时显示证据链构建过程
3. 展示篡改检测 → 修改一个数据点 → 裁决翻转
4. 导出.far-proof → 评委手机扫码独立验证

这个流程的任何一个环节都没连上。

### 致命问题3: "确定性裁决核"没有真实科学场景验证

22个anti-theater检测器是真实的统计逻辑——这点我验证了，它们不是空壳。
但：这些检测器只在Golden Vectors（人工构造的测试用例）上运行过。
没有任何一篇真实论文的claim被FAR-Lab验证过。

竞赛评委的第一个问题会是："除了你自己造的测试用例，你验证过任何真实的科学研究吗？"
回答"没有"=项目价值为零。

### 致命问题4: 项目的技术定位在竞争中不清晰

我搜索了同类竞赛和开源项目：
- MLPerf：AI基准测试（知名度极高）
- Weights & Biases：实验追踪（用户基数大）
- HuggingFace Evaluate：模型评估（社区庞大）
- ReplicateML / ScienceAgentFinder / PubGuard：科学诚信工具（学术前沿）

FAR-Lab的定位是"AI4S claim验证层"。但：
- 没有与上述任何项目的对比分析
- 没有解释为什么需要一个新的验证层而不是扩展现有工具
- "测谎仪"的比喻在中文语境有效但英文翻译会失去冲击力

### 致命问题5: 技术栈"看起来不前沿"

评委看到package.json里的依赖（Fastify + better-sqlite3 + zod）会想"这是一个标准的Web应用"。
虽然anti-theater检测器是前沿的统计逻辑，但这被埋在了CLI里。
前端用了React+D3——2020年的技术栈。没有任何2024-2025年的前沿技术信号。

## 解决方案：不是补丁，是能力升级

### 机制1: 强制"声称-现实"验证协议

每当我引用一个项目数据（"1484个测试""frontend存在""24个ADR"），
必须附带验证证据（具体命令+实际输出）。
这通过一个post_tool_call hook实现——当我声称某事时提醒我验证。

### 机制2: 强制"5层深挖"协议

发现一个问题后，不立即修复，而是强制深挖5层：
- L1: 表面问题（"测试浅薄"）
- L2: 同类问题（"代码是否也浅薄？"）
- L3: 根因（"为什么整体质量不够深？"）
- L4: 影响（"这对竞赛意味着什么？"）
- L5: 行动（"需要什么层级的干预？"）

### 机制3: 强制"竞争对手对照"协议

每个设计决策必须回答："MLPerf/W&B/HF Eval为什么没做这个？他们不做是因为不需要还是做不到？"

### 机制4: 反幻觉门控

每当我写"已完成""可用""存在"时，hook要求提供证据。
没有证据的声称被标记为UNVERIFIED。
