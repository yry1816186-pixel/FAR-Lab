# Secrets and local credentials
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# Dependency/build/test output (technology-specific rules may replace this later)
node_modules/
dist/
build/
coverage/
.cache/
.tmp/
tmp/
*.log

# Isolated external-source research clones/staging (covered by /research/ below)

# OS/editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/*.log

# Browser automation temp snapshots
.playwright-mcp/

# Local research-run artifacts (export explicitly when needed)
.far-run/

# ZCode runtime-extracted local marketplace copy (runtime artifact, not source)
FAR-Lab_ZCode_Control_Plane_*/

# Python lint cache (self-ignores, but explicit for robustness)
.ruff_cache/

# Tauri/Rust build output (desktop/, product session)
desktop/src-tauri/target/

# Public-release export output (local build artifact, never committed)
build/

# EEL sidecar venv (uv-managed, machine-local)
experiment-runtime/.venv/

# ZCode session-local state (plans, memories, runtime artifacts)
.zcode/

# Control-plane runtime state (execution state, decisions, blockers — local only)
.control/

# Eval run outputs (generated results, databases, temp builds)
eval/results/
*.db
*.db-wal
*.db-shm

# ---- Repo governance (2026-08-28): GitHub carries source + necessary files only. ----
# Workspace process artifacts stay on disk locally; never uploaded.
/evidence/
/artifacts/
/research/
/.planning/
/spikes/

# Transient runtime junk
*.err
/test-results/
/playwright-report/
web/test-results/
/.far-hx-before/
/.tmp-far-hx-isolated/
/far-run/

# Workspace-internal process/handoff docs (single source of truth is local)
/START_HERE.md
/BUNDLE_MANIFEST.json
/ach-matrix.yml
/ach-matrix-v2.yml
/final_delivery.md
/PROGRESS.md
/HANDOFF_PROMPT.md
/FINAL_BUILD_PROMPT.md
/FAR-Lab_DEVELOPMENT_MISSION.md
/FAR-Lab_HUMAN_EXPERIENCE_LAYER_FINAL_RECONSTRUCTION_PROMPT.md

# External copyrighted material (third-party paper, never upload)
/jss_metafor.pdf

# Nested git worktrees (isolated lanes)
work/
