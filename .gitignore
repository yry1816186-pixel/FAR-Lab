# Secrets and local credentials
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# Dependency/build/test output (technology-specific rules may replace this later)
node_modules/
dist/
build/
coverage/
.cache/
.tmp/
tmp/
*.log

# Isolated external-source research clones/staging
research/clones/
research/staging/

# OS/editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/*.log

# Browser automation temp snapshots
.playwright-mcp/

# Local research-run artifacts (export explicitly when needed)
.far-run/

# ZCode runtime-extracted local marketplace copy (runtime artifact, not source)
FAR-Lab_ZCode_Control_Plane_*/

# Python lint cache (self-ignores, but explicit for robustness)
.ruff_cache/

# Tauri/Rust build output (desktop/, product session)
desktop/src-tauri/target/

# Public-release export output (local build artifact, never committed)
build/

# EEL sidecar venv (uv-managed, machine-local)
experiment-runtime/.venv/

# ZCode session-local state (plans, memories, runtime artifacts)
.zcode/

# Control-plane runtime state (execution state, decisions, blockers — local only)
.control/

# Eval/spike run outputs (generated results, databases, temp builds)
eval/results/
spikes/**/output/
spikes/**/runs/
spikes/**/results/
spikes/**/data/
spikes/**/tmp-*/
*.db
*.db-wal
*.db-shm

# Nested git worktrees (isolated lanes)
work/
