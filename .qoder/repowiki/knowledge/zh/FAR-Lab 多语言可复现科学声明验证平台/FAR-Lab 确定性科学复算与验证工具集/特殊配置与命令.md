包元数据位于 `far_lab_repro.egg-info/PKG-INFO`，安装时可通过 `pip install .[dev]` 或 `pip install .[science]` 启用可选依赖；math backends 必须作为独立脚本通过 `python sympy_backend.py` / `python z3_backend.py` 调用，不可作为模块导入；`sandbox_runner.py` 要求宿主已创建隔离 venv 并通过 stdin JSON 传入待执行脚本。