- 所有跨模块质量门禁统一通过根 Makefile 目标（bootstrap/verify/demo/test/test-py）编排，而非各自独立脚本
- TS 与 Python 测试均通过 scripts/ensure_*_deps.mjs 在运行前自动补齐原生/Python 依赖，保证 CI 零配置启动
- 跨语言一致性通过 golden_vectors/ 中的确定性金向量（GV-*、VV-*）进行对拍验证，而非随机断言
- 数据契约由 schema_dir/ 的 JSON Schema/OpenAPI/SQLite 迁移单一维护，前端、API、tests 均从该处消费而非各自定义