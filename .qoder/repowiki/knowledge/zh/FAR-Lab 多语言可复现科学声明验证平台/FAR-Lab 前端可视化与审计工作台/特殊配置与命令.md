开发启动 npm run dev（Vite 默认端口 5173，自动代理 /api/v1、/health、/ready 到 http://localhost:3000）；构建命令 npm run build 会先执行 tsc -b 再 vite build，目标 es2022；测试运行 npm run test（jsdom 环境，src/test-setup.ts 注入 webcrypto/matchMedia/mock fetch）；类型检查 npm run typecheck；lint npm run lint。生产部署可通过设置 VITE_API_BASE_URL 环境变量切换为跨 origin 模式。