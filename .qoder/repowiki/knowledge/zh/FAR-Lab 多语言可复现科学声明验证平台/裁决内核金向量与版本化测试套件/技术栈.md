TypeScript 脚本（Node.js fs/path）调用项目内 `src/evidence_log/hasher.ts` 的 `canonicalHashVerified` / `hashCanonicalJson` 实现 SHA-256 规范化哈希；通过 stdin 与 Python 端 `hash_canonical_json` 进行跨语言 byte-equal 对拍（见 cross_lang_consistency.test.ts）。