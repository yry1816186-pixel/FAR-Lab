根目录作为多语言工作区入口，以 pnpm-workspace.yaml 管理 Node 依赖（仅允许 better-sqlite3 原生构建），并通过 Makefile 的 bootstrap / verify / demo / test / test-py 等目标串联 TypeScript 类型检查、ESLint、Node 测试、Python 回归测试以及 cross_lang_consistency 对拍；package.json 暴露 far CLI（src/cli/far.ts）并集中注册所有 tests/* 子集脚本，scripts/* 中的 ensure_native_deps.mjs、ensure_py_deps.mjs、run_py_tests.mjs 等桥接 Node 与 Python 环境；pyproject.toml 将 repro/ 打包为 far-lab-repro 包，约束 numpy/sympy/z3/threadpoolctl 版本以保证 R9 BLAS 线程数与确定性哈希；golden_vectors/ 提供 GV-01..14 金向量供 TS/Python 测试对拍，schema_dir/ 的 JSON Schema 与迁移脚本被前端、API、tests 共同消费；ci/ 与 scripts/ 共同构成持续集成门控，确保 Merkle 链、证据链自验与 Qwen 连通性。