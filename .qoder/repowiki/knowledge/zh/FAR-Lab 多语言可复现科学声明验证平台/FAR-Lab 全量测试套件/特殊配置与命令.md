需要预先安装 Python 解释器（findPythonCommand 在 Windows 优先 `python`，Unix 优先 `python3`）且可通过 `.python-deps` 与 `repro` 目录解析 numpy；tests/_helpers/benchmark_report.ts 会在首次运行时执行 `pnpm benchmark:generate` 生成 benchmark_report.json；所有测试从仓库根目录运行以便相对路径解析 repro/.python-deps。