# FAR-Lab 竞赛级交付策略 v1.0

> 本文档不是又一个"规划文档"——它是对项目现状的诚实诊断和从根上解决问题的行动方案。

## 第一部分：诚实诊断

### 1.1 项目实际拥有的（真实的）

FAR-Lab的核心价值主张是真实的，不是PPT概念：
- 22个anti-theater检测器，每个都有真实的统计欺诈检测逻辑（optional_stopping检测器实现了spending function分类+alpha消耗审计逻辑，不是空壳）
- 6-stage FSM agent loop + verdict kernel（确定性裁决核，非LLM裁决）
- 完整的CLI工具链（far demo/ask/verify/export/audit等25个命令全部可用）
- .far-proof便携验证包（export→verify→tamper→detect的完整信任链）
- 2023个测试（2017 pass / 0 fail / 6 skip），其中约80%有实质断言

### 1.2 项目实际缺失的（致命的）

以下不是"nice to have"，是竞赛落榜的直接原因：

**A. 没有"惊艳时刻"**
当前demo是命令行输出文本。评委30秒内看不到任何让他们"WOW"的东西。竞赛项目的第一印象决定生死。需要一个60秒内让人理解"AI4S测谎仪"价值的可视化演示。

**B. 没有外部验证**
2023个测试全部是自己写的。没有任何第三方使用过这个系统。竞赛评委第一个问题会是"除了你自己，谁用过？"零外部验证=零可信度。

**C. 没有真实科学案例**
当前demo用的是TESS脉冲星数据（天文）。但没有一个完整的、从真实论文假设到FAR-Lab验证到结论的端到端案例。评委需要看到"这个系统发现了真实研究中的一个问题"。

**D. 技术先进性不够清晰**
依赖列表是Fastify + better-sqlite3 + zod。这些都是成熟的传统Web技术栈。竞赛评委看到这个技术栈会想"这跟2024年的AI agent有什么关系？"需要在技术栈中体现前沿性。

**E. 文档自嗨**
33个设计文档全部是中文，大量术语自创（"测谎仪/三问/确定性规则核"）。竞赛评委（可能是国际评委）读不懂=不存在。

### 1.3 测试质量问题（你指出的）

量化诊断：
- 203个测试文件，38K行，4933个断言
- 12 assertions/100行（中等偏上，但不够优秀）
- 11个文件断言密度低于1/20行（空壳测试）
- 核心问题不是数量而是验证深度：大量测试验证"函数不崩溃"而不是"逻辑正确"

## 第二部分：策略定义

### 2.1 核心定位重定义

当前定位："AI4S结论的测谎仪"

问题：太抽象，评委不知道你在测什么谎。

重新定位：**"AI科研诚信的护栏——让LLM生成的科学假设可被独立验证、可被篡改检测、可被任何人复算"**

具体化三步：
1. LLM提出一个科学假设（如"化合物X的催化效率优于Y"）
2. FAR-Lab用确定性规则核验证这个假设的证据链
3. 输出一个可携带的.far-proof包，任何人独立复算验证结果

### 2.2 五大战略 pillar

#### Pillar 1: 惊艳演示层（最高优先级）

目标：60秒内让评委理解并震惊。

方案：
- 一个交互式Web演示，左侧是LLM提出的假设+证据链，右侧是FAR-Lab的裁决过程实时可视化
- 演示篡改场景：修改一个数据点→裁决立刻从CONFIRMED翻转为REFUTED
- 演示复算场景：评委拿手机扫码→在自己的浏览器里独立验证结果
- 技术实现：React/Next.js前端 + FAR-Lab API后端 + WebSocket实时裁决流

#### Pillar 2: 真实科学案例（差异化核心）

目标：证明FAR-Lab不只是技术demo，是真正能发现科学问题的工具。

方案：
- 选取3个真实已发表论文的claim（覆盖天文/材料/生物）
- 用FAR-Lab验证这些claim
- 如果发现其中某些claim的证据链有问题（p-hacking/optional stopping/cherry-picking），这就是最有力的演示
- 如果全部pass，证明FAR-Lab能为已发表研究提供独立验证证书

#### Pillar 3: 技术前沿性（防过时策略）

目标：让评委看到这个项目不只是现在的技术，是面向未来的基础设施。

方案：
- 评估协议层标准化：将FAR-Lab的.far-proof格式提案到W3C Verifiable Credentials或同类标准
- Agent-agnostic设计：明确展示FAR-Lab不依赖任何特定LLM——同一个claim用GPT-4o/Claude/GLM生成，FAR-Lab给出一致的裁决
- 可组合性：展示FAR-Lab可以作为插件集成到现有AI4S工作流（MLflow/Weights&Biases/HuggingFace）

#### Pillar 4: 质量深度（从覆盖率转向验证深度）

目标：消灭空壳测试，每个测试验证一个真实的逻辑分支。

方案——测试有效性审计协议（替代覆盖率指标）：
- 定义"验证深度等级"：L1(调用了函数) / L2(检查了返回值) / L3(验证了边界条件) / L4(验证了不变量) / L5(对抗性测试：尝试打破不变量)
- 每个测试文件标注其验证深度等级
- 目标：零L1测试，80%+为L3以上
- 工具：写一个脚本扫描每个assertion的语义深度

#### Pillar 5: 国际化+文档（可评審性）

目标：评委能读懂、能验证、能复现。

方案：
- 全部对外文档双语化（中文+英文）
- 写一份"评委快速验证指南"：5分钟内从clone到看到结果
- 录制一个3分钟的演示视频（英文）
- CI badge真实化：绿CI、真实coverage数字

### 2.3 防过时策略

这是你最担心的问题。解决方案不是追技术潮流，是让项目的核心价值独立于任何特定技术：

**核心不变量（无论AI技术如何变化都成立）：**
1. "LLM会幻觉"——这个事实不会因为模型变强而消失（GPT-5也会幻觉，只是更少）
2. "科学需要可复现"——这是科学的 foundational principle，永远不变
3. "信任需要第三方验证"——这是人类社会的 foundational principle，永远不变

**技术解耦设计：**
- LLM层：当前用DashScope/Qwen，但接口是OpenAI-compatible，任何LLM可替换
- 评估层：纯确定性代码，不依赖任何AI能力
- 验证包：.far-proof是纯JSON+hash，任何技术栈可读取
- Agent loop：FSM设计，不依赖特定agent框架

**竞争壁垒（不依赖技术先进性的壁垒）：**
- 22个anti-theater检测器的领域知识积累——这需要统计学+科学方法论的专业知识，不是技术能力
- 确定性裁决核的规则体系——这是FAR-Lab的核心IP，模仿者需要理解每条规则的统计学原理

## 第三部分：执行计划

### Phase 0: 清零与重建基础 (1-2天)
- [0.1] 测试有效性审计：标注所有203个测试文件的验证深度等级，删除/重写所有L1测试
- [0.2] 删除所有未实现的设计文档对应的空壳代码
- [0.3] 统计实际可用功能清单 vs 设计文档声明

### Phase 1: 真实科学案例 (3-5天)
- [1.1] 选取3篇真实论文，提取claims
- [1.2] 用FAR-Lab验证，记录过程和结果
- [1.3] 如果发现问题，写案例报告

### Phase 2: 惊艳演示 (3-5天)
- [2.1] 设计交互式Web演示UI
- [2.2] 实现篡改检测实时可视化
- [2.3] 实现扫码独立验证
- [2.4] 录制演示视频

### Phase 3: 质量深度 (持续)
- [3.1] 重写所有L1/L2测试为L3+
- [3.2] 添加对抗性测试（尝试打破不变量）
- [3.3] CI gate升级：测试深度报告

### Phase 4: 国际化+文档 (2-3天)
- [4.1] 对外文档双语化
- [4.2] 评委验证指南
- [4.3] 演示视频（英文）

### Phase 5: 竞争定位 (1-2天)
- [5.1] 撰写competitive analysis（vs MLflow/W&B/HuggingFace Eval）
- [5.2] 明确FAR-Lab的独特位置
- [5.3] 准备答辩材料

## 第四部分：防偷懒机制

### 问题根源
Agent偷懒是因为优化目标是"覆盖率"和"文件数"——这些指标可以通过灌水达成。

### 解决方案：验证深度门控

**新规则（写入hook强制执行）：**

1. 每个测试文件必须在头部注释中声明验证深度等级
2. 测试提交前运行`scripts/test_depth_audit.mjs`——检查每个assertion的实际语义
3. L1测试（只调用不验证）直接拒绝合并
4. 目标指标从"覆盖率"改为"L3+测试占比"和"对抗性测试数"

**验证深度定义：**
- L1: `expect(fn()).toBeDefined()` — 只证明函数存在
- L2: `expect(fn(input)).toBe(output)` — 验证了happy path
- L3: `expect(fn(edgeInput)).toThrow()` — 验证了边界/错误
- L4: `expect(fn(tamperedInput)).rejects()` — 验证了不变量
- L5: `for adversarialInput in attackCorpus: expect(fn(adversarialInput)).detectAttack()` — 对抗性测试

### 工作产出验收协议

每个worker任务完成时必须提供：
1. 变更的文件列表 + git diff
2. 运行的验证命令 + 实际输出（不是"通过"，是exit code + 数字）
3. 如果是新代码：验证深度等级声明
4. 如果是文档：对应的代码引用（file:line）
